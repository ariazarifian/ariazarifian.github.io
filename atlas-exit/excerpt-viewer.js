(()=>{'use strict';const chunks=window.ATLAS_EXCERPT_CHUNKS||[];const status=document.getElementById('status');const open=document.getElementById('openPdf');const frame=document.getElementById('pdfFrame');try{if(chunks.length!==4||chunks.some(x=>!x))throw new Error('Extrait incomplet');const b64=chunks.join('');const raw=atob(b64);const bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);const blob=new Blob([bytes],{type:'application/pdf'});const url=URL.createObjectURL(blob);open.href=url;open.target='_blank';open.rel='noopener noreferrer';frame.src=url;status.textContent='Extrait gratuit · 5 pages réelles du guide';window.addEventListener('beforeunload',()=>URL.revokeObjectURL(url));}catch(err){status.textContent='Impossible de charger l’extrait.';open.hidden=true;frame.hidden=true;document.getElementById('fallback').hidden=false;}})();