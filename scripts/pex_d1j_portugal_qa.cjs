const assert=require('node:assert/strict'),fs=require('node:fs');const{chromium}=require('playwright');
const PRODUCT='99a90012fd25c75d112be4f6dc63ab848d738928',BASE='abafbe9ec7777b65f68c39ae6fab024be25abaae',URL='http://127.0.0.1:8000/atlas-exit/index.html';
const EXPECTED=['https://info.portaldasfinancas.gov.pt/en/tax-information/getting-started-in-portugal/tax-residency/tax-residency-rules/Pages/default.aspx','https://www.gov.pt/guias/imposto-sobre-o-rendimento-das-pessoas-singulares-irs-em-portugal','https://portugal.gov.pt/pt/gc25/governo/comunicados-do-conselho-de-ministros/comunicado-do-conselho-de-ministros-de-17-de-setembro-de-2026','https://www.parlamento.pt/ActividadeParlamentar/Paginas/DetalheIniciativa.aspx?BID=377272','https://www.oe.gov.pt/financas-a-lupa/artigos/o-que-e-o-irc-e-quem-tem-de-o-pagar/','https://www.gov.pt/guias/imposto-sobre-valor-acrescentado-iva-em-portugal','https://www.ine.pt/ine_novidades/semin/INEWS60/78-79/','https://www2.gov.pt/en/servicos/pedir-o-certificado-de-registo-para-cidadao-da-ue/eee/suica','https://www2.gov.pt/en/servicos/pedir-o-numero-de-utente-do-sns','https://www2.gov.pt/en-GB/servicos/inscrever-se-no-centro-de-saude','https://www.diplomatie.gouv.fr/fr/information-par-pays/portugal/conseils-aux-voyageurs-securite'];
const report={productHead:PRODUCT,baseHead:BASE,viewports:[],errors:[]};
async function choose(page,name,id,panelSel){await page.locator('#countrySearch').fill(name);await page.waitForTimeout(180);const row=page.locator(`[data-country="${id}"]`).first();await row.waitFor({state:'attached',timeout:10000});await row.evaluate(el=>el.click());await page.waitForSelector(panelSel,{timeout:10000});return page.locator(panelSel);}
async function openOuter(panel){if((await panel.getAttribute('open'))===null)await panel.locator(':scope > summary').click();assert.notEqual(await panel.getAttribute('open'),null);}
async function noOverflow(page){assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1),true);assert.equal(await page.locator('#inspector').evaluate(el=>el.scrollWidth<=el.clientWidth+1),true);}
(async()=>{const browser=await chromium.launch({headless:true});for(const vp of [{name:'desktop',width:1440,height:1000},{name:'mobile',width:390,height:844}]){const ctx=await browser.newContext({viewport:{width:vp.width,height:vp.height}}),page=await ctx.newPage(),errs=[];page.on('pageerror',e=>errs.push(String(e)));page.on('console',m=>{if(m.type()==='error'&&!/umami|ERR_FAILED|Failed to load resource/i.test(m.text()))errs.push('console:'+m.text())});page.on('requestfailed',r=>{if(new globalThis.URL(r.url()).origin==='http://127.0.0.1:8000')errs.push('requestfailed:'+r.url())});page.on('response',r=>{if(new globalThis.URL(r.url()).origin==='http://127.0.0.1:8000'&&r.status()>=400)errs.push('http'+r.status()+':'+r.url())});await page.goto(URL,{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>window.AtlasExplorer?.ready?.()&&window.ATLAS_COUNTRY_EVIDENCE_PORTUGAL?.validation?.ok===true,null,{timeout:15000});await page.waitForFunction(()=>window.AtlasExplorer.getCountries().find(c=>c.id==='PRT')?.tax===null,null,{timeout:10000});assert.equal(await page.evaluate(()=>document.fonts.check('12px Manrope')),true);assert.equal(await page.locator('text=Découvrir').count(),0);assert.equal(await page.locator('label[for="stabilityRange"]').innerText(),'Stabilité politique');assert.equal(await page.locator('[data-layer="conflict"]').innerText(),'Conflit & tensions');await noOverflow(page);if(vp.name==='mobile'){await page.locator('#mobileFilters').click();assert.equal(await page.locator('#mobileFilters').getAttribute('aria-expanded'),'true');}
const contract=await page.evaluate(()=>{const e=ATLAS_COUNTRY_EVIDENCE,g=e.countries.PRT,c=AtlasExplorer.getCountries().find(x=>x.id==='PRT');return{keys:Object.keys(g.fields),required:e.requiredFields,states:Object.values(g.fields).map(x=>x.state),counts:Object.fromEntries(Object.entries(g.fields).map(([k,v])=>[k,e.sourcesFor(v).length])),locators:Object.values(g.fields).flatMap(v=>e.sourcesFor(v).map(s=>s.locator)),tax:c.tax,taxLabel:c.taxLabel,pitFresh:g.fields.pit.freshness,pit:g.fields.pit.structure,corp:g.fields.cit_business.structure,vat:g.fields.consumption_tax.structure,cost:g.fields.cost_context.structure,res:g.fields.tax_residency.structure,visa:g.fields.residence_visa.structure,health:g.fields.healthcare.structure,safety:g.fields.safety_context.structure};});assert.deepEqual(contract.keys,contract.required);assert.equal(contract.states.filter(x=>x==='READY_FOR_PRODUCT').length,8);assert.deepEqual(contract.counts,{tax_residency:1,pit:3,cit_business:1,consumption_tax:1,cost_context:1,residence_visa:1,healthcare:2,safety_context:1});assert.deepEqual(contract.locators,EXPECTED);assert.equal(contract.tax,null);assert.match(contract.taxLabel,/OE2026/);assert.equal(contract.pitFresh.state,'WATCH');assert.equal(contract.pitFresh.checkedOn,'2026-09-23');assert.match(contract.pitFresh.trigger,/enacted|rejected|Diário|final rates/i);assert.equal(contract.pit.oe2026OperativeBaseline,true);assert.equal(contract.pit.proposal17SepSettledLaw,false);assert.equal(contract.pit.finalProposalRatesHardcoded,false);assert.equal(contract.res.taxResidenceSeparateFromImmigration,true);assert.equal(contract.corp.allInScalarForbidden,true);assert.equal(contract.vat.portugalWideScalarForbidden,true);assert.equal(contract.cost.expatBudget,false);assert.equal(contract.cost.singlePersonBudget,false);assert.equal(contract.visa.visaRoute,false);assert.equal(contract.visa.taxResidenceSeparate,true);assert.equal(contract.health.snsNumberMeansAllServicesFree,false);assert.equal(contract.safety.scalarScoreForbidden,true);
let panel=await choose(page,'Portugal','PRT','[data-atlas-portugal-evidence][data-country="PRT"]');assert.equal(await panel.locator('[data-evidence-field]').count(),8);assert.equal(await panel.locator('a.detail-source').count(),11);assert.equal(await panel.getAttribute('open'),null);await openOuter(panel);assert.ok((await panel.locator('.atlas-evidence__row>p').evaluateAll(es=>es.map(e=>getComputedStyle(e).display))).every(x=>x==='none'));assert.ok((await panel.locator('.atlas-evidence__source').evaluateAll(es=>es.map(e=>e.hasAttribute('open')))).every(x=>x===false));const compact=await panel.innerText();for(const re of [/183 jours/,/OE2026/,/19 % continent/,/23 %/,/23 900 €/,/Citoyen UE/,/SNS/,/Vigilance normale/])assert.match(compact,re);const pitSource=panel.locator('[data-evidence-field="pit"] .atlas-evidence__source');await pitSource.locator('summary').click();assert.equal(await pitSource.locator('a.detail-source').count(),3);assert.match(await pitSource.innerText(),/ne sont pas intégrées|settled|texte final|final law/i);await noOverflow(page);await page.locator('[data-save="PRT"]').click();assert.equal(await page.locator('#savedCount').textContent(),'1');
panel=await choose(page,'Allemagne','DEU','[data-atlas-germany-evidence][data-country="DEU"]');assert.equal(await panel.locator('[data-evidence-field]').count(),8);assert.equal(await panel.locator('a.detail-source').count(),10);await noOverflow(page);await page.locator('[data-save="DEU"]').click();assert.equal(await page.locator('#savedCount').textContent(),'2');panel=await choose(page,'Royaume-Uni','GBR','[data-atlas-united-kingdom-evidence][data-country="GBR"]');assert.equal(await panel.locator('[data-evidence-field]').count(),8);assert.equal(await panel.locator('a.detail-source').count(),11);await noOverflow(page);await page.locator('[data-save="GBR"]').click();assert.equal(await page.locator('#savedCount').textContent(),'3');panel=await choose(page,'Canada','CAN','[data-atlas-canada-evidence][data-country="CAN"]');assert.equal(await panel.locator('[data-evidence-field]').count(),8);await openOuter(panel);assert.ok((await panel.locator('.atlas-evidence__row>p').evaluateAll(es=>es.map(e=>getComputedStyle(e).display))).every(x=>x==='none'));await noOverflow(page);
await page.locator('[data-layer="conflict"]').evaluate(el=>el.click());await page.waitForFunction(()=>document.querySelector('[data-layer="conflict"]')?.getAttribute('aria-pressed')==='true');await choose(page,'Japon','JPN','[data-conflict-panel]');let conflictTxt=await page.locator('[data-conflict-panel]').innerText();assert.match(conflictTxt,/modéré/i);await choose(page,'Thaïlande','THA','[data-conflict-panel]');conflictTxt=await page.locator('[data-conflict-panel]').innerText();assert.match(conflictTxt,/modéré/i);assert.match(conflictTxt,/Cambodge/);await page.locator('[data-layer="tax"]').evaluate(el=>el.click());
await page.locator('#countrySearch').fill('Portugal');await page.waitForTimeout(150);assert.equal(await page.locator('[data-country="PRT"]').count(),1);await page.locator('#countrySearch').fill('');await page.locator('[data-region="Europe"]').evaluate(el=>el.click());await page.waitForTimeout(120);assert.ok(Number((await page.locator('#resultCount').textContent()).match(/\d+/)?.[0]||0)>0);await page.locator('#resetFilters').evaluate(el=>el.click());const z0=await page.locator('#zoomValue').textContent();await page.locator('#zoomOut').evaluate(el=>el.click());await page.waitForTimeout(350);const z1=await page.locator('#zoomValue').textContent();assert.notEqual(z1,z0);await page.locator('#zoomIn').evaluate(el=>el.click());await page.waitForTimeout(350);const z2=await page.locator('#zoomValue').textContent();assert.notEqual(z2,z1);await page.locator('#openCompare').evaluate(el=>el.click());await page.locator('#compareDialog').waitFor({state:'visible',timeout:10000});await page.waitForTimeout(180);const cmp=await page.locator('#compareTable').innerText();assert.match(cmp,/Portugal/);assert.match(cmp,/Allemagne/);assert.match(cmp,/Royaume-Uni/);assert.match(cmp,/Progressif · base OE2026/);assert.match(cmp,/19 % continent\/Madère/);assert.match(cmp,/23 % standard continental/);assert.ok(await page.locator('#compareTable [data-atlas-normalized="PRT"]').count()>=5);await page.locator('#compareDialog [data-close]').evaluate(el=>el.click());await noOverflow(page);
for(const [path,checks] of [['offres.html',['Guide V15 gratuit','free-guide/usa','guide_download_click','free_funnel_cta','parcours-usa.html']],['parcours-usa.html',['atlas-events.js','USCIS','Roadmap']],['accompagnement.html',['Guide V15','Roadmap','free_funnel_cta']]]){const p=await ctx.newPage();const local=[];p.on('pageerror',e=>local.push(String(e)));p.on('requestfailed',r=>{if(new globalThis.URL(r.url()).origin==='http://127.0.0.1:8000')local.push('requestfailed:'+r.url())});await p.goto('http://127.0.0.1:8000/atlas-exit/'+path,{waitUntil:'domcontentloaded'});const html=await p.locator('html').innerHTML();for(const s of checks)assert.ok(html.includes(s),path+' missing '+s);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1),true);assert.deepEqual(local,[]);await p.close();}
fs.mkdirSync('qa-artifacts',{recursive:true});await page.screenshot({path:`qa-artifacts/pex-d1j-portugal-${vp.name}.png`,fullPage:true});report.viewports.push({...vp,fields:8,sourceLinks:11,pitFreshness:'WATCH',checkedOn:'2026-09-23',portugal:true,germanyRegression:true,ukRegression:true,canadaCompact:true,japanConflict:true,thailandConflict:true,zoom:{start:z0,out:z1,in:z2}});report.errors.push(...errs.map(e=>vp.name+':'+e));assert.deepEqual(errs,[]);await ctx.close();}await browser.close();fs.writeFileSync('qa-artifacts/report.json',JSON.stringify(report,null,2));console.log(JSON.stringify(report,null,2));})().catch(e=>{console.error(e);process.exit(1)});
